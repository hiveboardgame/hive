DATABASE_URL="postgres://hive-dev@localhost:/hive-local"
TEST_DATABASE_URL="postgres://hive-dev@localhost:/hive-test"
COOKIE_SECRET_KEY="top secret"
JWT_SECRET_KEY="secret"
HIVE_HYDRA_BOT_NOKAMUTE_EASY_PASSWORD="hivegame"
HIVE_HYDRA_BOT_NOKAMUTE_MEDIUM_PASSWORD="hivegame"
HIVE_HYDRA_BOT_NOKAMUTE_HARD_PASSWORD="hivegame"

# Absolute path to the Firebase Admin SDK service-account JSON, used by the
# push-notification dispatcher to send messages via FCM. Empty in this
# committed file (dev runs without push); set locally / on prod to the real
# path. File itself MUST stay outside git — see `*-firebase-adminsdk-*.json`
# rule in /.gitignore.
GOOGLE_APPLICATION_CREDENTIALS="/Users/leex/.config/hive/hivegame-59cc6-firebase-adminsdk-fbsvc-2158e49efb.json"
